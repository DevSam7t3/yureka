import { copyFile, lstat, mkdir, readFile, readdir, writeFile } from "fs/promises";
import path from "path";
const copyFilesAndDirectories=async(a,e)=>{const i=await readdir(a);for(const t of i){const i=path.join(a,t),o=path.join(e,t);(await lstat(i)).isDirectory()?(await mkdir(o),await copyFilesAndDirectories(i,o)):await copyFile(i,o)}},renamePackageJsonName=async(a,e)=>{const i=path.join(a,"package.json");try{const a=await readFile(i,"utf8"),t=JSON.parse(a);t.name=e,await writeFile(i,JSON.stringify(t,null,2),"utf8")}catch(a){console.log(a.message)}};export { copyFilesAndDirectories, renamePackageJsonName };
